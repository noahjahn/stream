// Magic strings, arrays, and objects

export const MAGIC_STRING = 'uploads'

export const MAGIC_ARRAY = ['user', 'manager', 'admin'] as const

export const MAGIC_OBJECT = {
  id: 'fake-uuid',
  name: 'John Smith'
} as const
